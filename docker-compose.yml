version: "3.8"
services:
  frontend:
    image: vue-frontend/avh:latest
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - "3000:3000"
    command: bash -c "npm install && npm run dev"
    volumes:
      - .:/usr/src/app